apiVersion: v1
kind: Service
metadata:
  name: prometheus-service
  namespace: fraud-mlops
spec:
  type: ClusterIP
  selector:
    app: prometheus
  ports:
    - name: http
      port: 9090
      targetPort: 9090
