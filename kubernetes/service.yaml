apiVersion: v1
kind: Service
metadata:
  name: fraud-detection-service
  namespace: fraud-mlops
spec:
  type: NodePort
  selector:
    app: fraud-detection-api
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000

